package com.example.revelation_2026_test.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// ─────────────────────────────────────────────────────────────────────────────
// CHRIST UNIVERSITY / REVELATIONS 2026 COLLEGE THEME COLORS
// White, Cream, Blue, Dark Blue
// ─────────────────────────────────────────────────────────────────────────────

// Primary colors - Blue shades
val DarkBlue = Color(0xFF0D1B4C)        // Deep navy blue (primary dark)
val MediumBlue = Color(0xFF1E3A8A)       // Royal blue (primary)
val LightBlue = Color(0xFF3B82F6)        // Bright blue (accent)
val SkyBlue = Color(0xFF60A5FA)          // Light sky blue

// Neutral colors - White and Cream
val White = Color(0xFFFFFFFF)             // Pure white
val Cream = Color(0xFFFFF8F0)             // Warm cream
val LightCream = Color(0xFFFFFBF5)        // Light cream background
val Ivory = Color(0xFFFFFFF0)             // Ivory white

// Gradient colors for backgrounds
val GradientStart = Color(0xFF0D1B4C)     // Dark blue
val GradientMid = Color(0xFF1E3A8A)       // Medium blue
val GradientEnd = Color(0xFF2563EB)       // Bright blue

// Surface and card colors
val CardWhite = Color(0xFFFFFFFF)
val CardCream = Color(0xFFFFF8F0)
val DarkSurface = Color(0xFF0F172A)       // Very dark blue-gray

// Accent colors
val AccentBlue = Color(0xFF2563EB)        // Button accent
val AccentGold = Color(0xFFD4AF37)        // Gold accent for highlights
val AccentLightBlue = Color(0xFF93C5FD)   // Light blue accent

// Feedback colors
val ErrorRed = Color(0xFFDC2626)
val SuccessGreen = Color(0xFF16A34A)
val WarningOrange = Color(0xFFF59E0B)

// Legacy aliases for compatibility (mapped to new theme)
val DeepPurple = DarkBlue
val ElectricBlue = MediumBlue
val MidnightPurple = DarkBlue
val NeonPink = AccentBlue
val VibrantCyan = LightBlue
val GoldenAccent = AccentGold
val LightCard = Cream